import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import ContextDetailApp from "./concepts/context-detail/ContextDetailApp.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ContextDetailApp />
  </React.StrictMode>
);
