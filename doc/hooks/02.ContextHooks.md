# Context hooks

React Context Hooks are a way to share data between components without having to pass props down manually at every level. This can be useful for data that is needed by many components throughout an application, such as the current user, locale, or theme.

## Why should use?

Something

## When not to use ?

Another something

## Basic steps to use React context

something

## Conclusion

Some conclution
