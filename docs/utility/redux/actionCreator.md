# Action creator

- Action

  ```javascript
  const incrementAction = { type: TYPE, payload: PAYLOAD };
  ```

- Action Creator:

  ```javascript
  const increment = (amount) => ({ type: TYPE, payload: amount });
  ```
